
/* Gaya umum */
* {
    text-decoration: none;
    margin: 0;
    padding: 0;
}

body {
    font-family: Arial;
    margin: 0;
    padding: 0;
    background-color: #7fa9b0;

}

header {
    background-color: #000000;
    color: #808080;
    padding: 15px;
    text-align: right;
}

nav a {
    color: #808080;
    margin: 15px;
    text-decoration: none;
    font-weight: bold;
    transition: color 0.3s;
}

nav a:hover {
    color: #0000FF;
    text-decoration: underline;
}

.section {
    display: none;
    min-height: 100vh;
    padding: 20px;
    text-align: center;
}

h1, h2, h3 {
    color: #3f5154;
    margin-bottom: 15px;
    margin-top: 0;
}

p {
    font-size: 1.1em;
    line-height: 1.6;
    color: #000000;
}

ul {
    list-style-type: none;
    padding: 0;
}

ul li {
    margin: 10px 0;
    text-align: left;
}

ul li a {
    color: #0000FF;
    text-decoration: none;
}

ul li a:hover {
    text-decoration: underline;
}

.foto-home {
    width: 800px; 
    height: auto; 
    border-radius: 10px;
    margin-right: 50px; /* Jarak antara gambar dan teks */
    float: left; /* Pindahkan gambar ke sudut kiri */
}

.home-content {
    display: flex; /* Gunakan flexbox untuk susun konten gambar dan teks */
    align-items: flex-start; 
    text-align: left; /* Pastikan teks tetap rata kiri */
    padding: 20px;
}

/* Untuk gambar diri */
.foto-gambar-diri {
    width: 300px; /* Perbesar ukuran gambar */
    height: auto; /* Jaga rasio gambar */
    border-radius: 10px;
    margin-right: 50px; /* Jarak antara gambar dan teks */
    float: left; /* Pindahkan gambar ke sudut kiri */
}

.section {
    display: flex; /* Gunakan flexbox untuk susun konten gambar dan teks */
    align-items: center; /* Posisikan konten secara vertikal rata */
    text-align: left; /* Pastikan teks tetap rata kiri */
    padding: 20px;
}

/* Penyesuaian untuk media sosial yang berderet dari kiri ke kanan */
.social-media-list {
    display: flex;
    justify-content: center; /* Pusatkan elemen */
    gap: 20px; /* Jarak antara item */
}

.social-media-list li {
    list-style: none;
}

.section img {
    margin-right: 20px;
}
